{{! This will be your default layout for all handlebars file rendered. This will be inherited by all other templates. }}
{{! Refer lecture code to learn more on how this works }}

<html>
  <head>
    <meta charset="utf-8" />
    <title>{{title}}</title>
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css?family=Quicksand:300,400,500,700&display=swap"
    />
    <style>
      body { font-family: 'Quicksand', sans-serif; background-color: #f2f2f2;
      margin: 0; padding: 0; display: flex; justify-content: center;
      align-items: center; height: 100vh; } .login-container, .signup-container,
      .forgot-password-container { background-color: #fff; border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); padding: 40px; width: 320px;
      text-align: center; } .login-container h1, .signup-container h1,
      .forgot-password-container h1, .welcome-container h1, .message-container
      h1 { font-size: 36px; color: #FF5733; margin-bottom: 20px; } .form-group {
      margin-bottom: 20px; text-align: left; } .form-group label { display:
      block; font-size: 16px; color: #555; margin-bottom: 5px; } .form-group
      input { width: 100%; font-size: 16px; padding: 10px; margin-top: 5px;
      border: 1px solid #ccc; border-radius: 5px; } .login-btn, .signup-btn,
      .change-password-btn { background-color: #FF5733; color: #fff; font-size:
      18px; padding: 10px 20px; border: none; border-radius: 8px; cursor:
      pointer; width: 100%; transition: background-color 0.3s ease; }
      .login-btn:hover, .signup-btn:hover, .change-password-btn:hover {
      background-color: #FF7043; } .error-msg { font-size: 16px; color: red;
      margin-top: 10px; } .links { margin-top: 20px; } .links a { font-weight:
      bold; color: #0077cc; text-decoration: none; margin-right: 10px;
      transition: color 0.3s ease; } .links a:hover { color: #0056b3; } .buttonn
      { font-family: 'Quicksand', sans-serif; font-size: 18px; text-decoration:
      none; color: #FF5733; border: 2px solid #FF5733; padding: 10px 20px;
      border-radius: 8px; background-color: #fff; transition: background-color
      0.3s, color 0.3s; cursor: pointer; } .buttonn:hover { background-color:
      #FF5733; color: #fff; } .welcome-container, .message-container {
      text-align: center; } .welcome-container p, .message-container h1 {
      font-size: 18px; color: #555; margin-bottom: 20px; }.login-link {
      font-weight: bold; color: #0077cc; text-decoration: none; margin-top:
      20px; display: inline-block; transition: color 0.3s ease; }
      .login-link:hover { color: #0056b3; }footer { position: fixed; bottom: 10px;
      width: 100%; text-align: center; margin-top: 20px; font-size: 16px; color:
      #555; }
    </style>
  </head>
  <body>
    <main>
      {{{body}}}
    </main>
    <footer>
      Made with ❤️ by Dhruti Patel
    </footer>
  </body>
</html>